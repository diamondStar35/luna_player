// Every user-visible string in the player is wrapped in Tr(), so the lookup is a global using rather than
// something each file has to remember to import.
global using static LunaPlayer.Configuration.Text;
